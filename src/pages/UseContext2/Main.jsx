import { H1 } from './H1';
import { P } from './P';

// eslint-disable-next-line
export const Main = ({ children }) => {
  return (
    <main className="main-container">
      <H1 />
      <P />
    </main>
  );
};
